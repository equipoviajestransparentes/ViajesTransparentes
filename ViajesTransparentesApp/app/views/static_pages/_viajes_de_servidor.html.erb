    <style>
      html, body {
       width:100%;
       padding: 0px;
       margin: 0px;
      }
      .vco-storymap {
       margin-bottom:25px;
      }
      #storymap1{
       width:100%;
       height:300px;
       border-top: 1px solid #999;
       border-bottom: 1px solid #999;
      }
      
    </style>

    <div id="storymap1"></div>
    <!-- JavaScript-->
    <script>
      var storymap1 = new VCO.StoryMap('storymap1', <%= @hash_storymap.to_json.html_safe %>, {use_custom_markers:true});
      
      window.onresize = function(event) {
       storymap1.updateDisplay();
      }
    </script>